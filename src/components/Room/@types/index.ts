export interface ImageBoxProps {
  data: string[];
  size?: [number, number];
}

export interface RoomCardProps {
  media?: React.ReactNode;
  overview?: React.ReactNode;
  detail?: React.ReactNode;
  bundleDetailText?: any
}
